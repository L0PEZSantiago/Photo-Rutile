liip_imagine:
    resolvers:
        default:
            web_path:
                web_root: '%kernel.project_dir%/public'
                cache_prefix: 'media/cache'

    filter_sets:
        creation_thumbnail:
            quality: 85
            filters:
                thumbnail:
                    size: [379, 505]
                    mode: outbound
